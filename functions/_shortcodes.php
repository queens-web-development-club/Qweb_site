<?php
/*
$shortcodes = array(
	'shortcode_name'	=>	function($atts) {
								$atts = shortcode_atts(array(
									'attt' => null
								), $atts);
								extract($atts);

							}
);


foreach($shortcodes as $shortcode => $function ) {
	
	if( $shortcode && $function )
		add_shortcode($shortcode, $function);
		
}
*/